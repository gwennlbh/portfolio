{
  "name": "portfolio",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "database": "ortfodb --config ./ortfodb.yaml --no-progressbar --scattered build works.json"
  },
  "dependencies": {
    "astro": "^5.1.1",
    "date-fns": "^4.1.0",
    "slug": "^10.0.0",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/slug": "^5.0.9"
  }
}
